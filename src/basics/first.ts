function welcome(name:string, date:Date) {
     console.log(`Learning Typescript from ${name} on date ${date.toDateString()}!`);
}

welcome("Chai aur Code , typescriptlang.org",new Date());

// code is mostly similar to js , in ts types are the main deal we can use : to declare which type of data variable and functions will use and return 